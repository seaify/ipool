const React = require('react');

let ProxyList = React.createClass({
  getInitialState() {
    return {};
  },

  componentDidMount() {
  },

  render() {
    return (
      <p>Hello, world!</p>
    );
  }
});

module.exports = ProxyList;
